{
  "buildCommand": "pnpm --filter @regiart/desktop build",
  "outputDirectory": "apps/desktop/dist",
  "installCommand": "npm i -g pnpm@9 && pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
